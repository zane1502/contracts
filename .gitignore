# Rust's output directory
target

# Local settings
.soroban
.stellar

# Deployed contract addresses (network-specific, not for version control)
.deployed/

# Private working documents (never commit)
IMPLEMENTATION_PLAN.md
issue.md
issues_v2.md
.github-issue-push-cursor
.issues-progress.json
push_issues.py
push-github-issues.sh